-#  This file is app/views/item/index.html.haml
%h1 All Inventory items

%table#inventory
  %thead
    %tr
      %th{:class => ("hilite" if @sort == 'title')}
        = link_to "Item Name", :sort => "title"
      %th Description
      %th{:class => ("hilite" if @sort == 'checked_out')}
        = link_to  "Checked Out", :sort => "checked_out"
      %th{:class => ("hilite" if @sort == 'quantity')}
        = link_to "Total Quantity", :sort => "quantity"
      %th{:class => ("hilite" if @sort == 'remaining')}
        = link_to "Remaining", :sort => "remaining"
      %th More Details
  %tbody
    - @item.each do |item|
      %tr
        %td= item.title
        %td= item.description
        %td= item.checked_out
        %td= item.quantity
        %td= (item.quantity - item.checked_out)
        %td= link_to "Details about #{item.title}", item_path(item)
